import { ruleTester, fixtureFile, fixtureProjectFile } from '../index.spec';
import { rule } from './return-observable';

// it(rule.create.name, () => {
//     ruleTester.run(rule.create.name, rule, {
//         invalid: [],
//         valid: [],
//     });
// });
